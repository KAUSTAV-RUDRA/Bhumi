import React from 'react';

const HeroSection = () => (
  <section className="hero">
    <h1>Bhumi - Educate. Empower. Transform.</h1>
    <p>Together we can transform education for every child in India.</p>
  </section>
);

export default HeroSection;